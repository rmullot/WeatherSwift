// Placeholder. This target is only here so SwiftPM has something to build;
// the real purpose is to vendor the swiftgen/swiftlint executables as dependencies.
@main struct BuildTools { static func main() {} }
